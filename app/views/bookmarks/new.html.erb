<h1>Bookmarks#new</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :movie_id, collection: @movies %>
  <%= f.input :comment %>
  <%= f.submit %>
<% end %>
<%= link_to "Back to list", list_path(@list) %>
