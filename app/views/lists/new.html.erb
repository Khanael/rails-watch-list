<div class="container">
  <h1>Create a list</h1>
  <div class="mx-auto w-50">
    <%= simple_form_for(@list) do |f| %>
      <%= f.input :name %>
      <%= f.input :photo, as: :file %>
      <%= f.submit %>
    <% end %>
  </div>
  <%= link_to "Back to list", lists_path %>
</div>
